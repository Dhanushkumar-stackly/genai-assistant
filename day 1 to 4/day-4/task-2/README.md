# Day 4 — Task 2: Validate Every Response

## Roadmap requirement
Parse model output and validate it against the expected output model.
Validation failures must be separated from model/network failures.

## Supplied implementation
No response-schema validation implementation was present in the supplied ZIP.

Important: the supplied `run_prompt_tests.py` validates **input values**, not
model responses, so it is kept under Task 4 where the prompt test runner
belongs rather than incorrectly labeling it as response validation.
