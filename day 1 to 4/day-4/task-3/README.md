# Day 4 — Task 3: Create a 10-Case Prompt Dataset

The roadmap requires representative, ambiguous, malformed, and empty inputs
with expected structure or labels.

The supplied dataset is:

`prompt_cases.json`

The source file is empty in the supplied ZIP, so no test cases were invented.
