# Day 3 — Task 2: Summarization Prompt

The roadmap task is **Implement a summarization prompt**.

Prompt file: `summarization.txt`.

`summarizer_runner.py` is included because it is the supplied runtime entry point for the summarization prompt. Its imported `app.core` modules were not present in the supplied ZIP.
