# Day 3 — Task 5: Create Representative Sample Inputs

The roadmap requires normal, long, ambiguous, and incomplete examples for
each of the three prompt tasks. The supplied sample inputs are organized here
so they remain separate from the prompt implementation files.

## Samples

- `classification/`
- `extraction/`
- `summarization/`

Each contains:
- `normal.txt`
- `long.txt`
- `ambiguous.txt`
- `incomplete.txt`

`sample.json` is the additional structured sample document supplied in the
project.
